config:
  required_env:
    - OPENAI_MODEL
    - TEMPERATURE
    - MAX_TOKEN_LENGTH
    - INPUT_FILE
    - OUTPUT_FILE
    - GLOSSARY_FILE
    - TRANSLATION_MAX_RETRIES
    - TRANSLATION_RETRY_BACKOFF_SECONDS
  notes: "Config import fails fast if any required variable missing"
